apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Keep the bonus deployment reproducible with one kubectl apply command.
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
