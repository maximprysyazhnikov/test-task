apiVersion: v1
kind: Service
metadata:
  name: app
spec:
  # ClusterIP keeps the workload private; Ingress is the public entry point.
  type: ClusterIP
  selector:
    app.kubernetes.io/name: app
  ports:
    - name: http
      port: 8080
      targetPort: http
